{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Executar trunfo.exe",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/trunfo.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb.exe",
      "setupCommands": [
        {
          "description": "Habilitar formatação padrão para gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
